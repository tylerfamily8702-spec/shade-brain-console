# Shared environment variables the containers read at startup.
# Copy to ~/.config/shade-ai/.env and fill in real values.
# DO NOT commit the real .env with actual secrets - this is a placeholder template only.

LITELLM_MASTER_KEY=changeme
OLLAMA_HOST=0.0.0.0
TAILSCALE_AUTHKEY=changeme
WEBUI_SECRET_KEY=changeme
